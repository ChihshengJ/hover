{
  "name": "hover-pdf",
  "version": "0.10.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.node.json",
    "check:layers": "node scripts/check_layering.mjs",
    "check": "npm run typecheck && npm run check:layers",
    "build": "vite build",
    "build:chrome": "TARGET=chrome vite build",
    "build:firefox": "TARGET=firefox vite build",
    "build:safari": "TARGET=safari vite build && bash scripts/build_safari.sh",
    "build:all": "npm run build:chrome && npm run build:firefox && npm run build:safari",
    "build:store": "STORE_BUILD=1 npm run build:all",
    "package": "npm run build:all && node scripts/package.mjs",
    "watch:chrome": "TARGET=chrome vite build --watch",
    "dev:firefox": "npm run build:firefox && web-ext run --source-dir dist/firefox --firefox-profile '/Users/zs_jin/Library/Application Support/Firefox/profiles/bncnexsu.default-release' & TARGET=firefox vite build --watch",
    "lint:firefox": "web-ext lint --source-dir dist/firefox",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.7",
    "typescript": "^7.0.2",
    "vite": "^7.3.1",
    "web-ext": "^10.3.0"
  },
  "dependencies": {
    "@embedpdf/engines": "^2.15.0",
    "@embedpdf/pdfium": "^2.15.0",
    "@leeoniya/ufuzzy": "^1.0.19"
  }
}
