{
  "name": "hover-pdf-viewer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:ext": "vite build && npm run copy-ext-files",
    "copy-ext-files": "cp manifest.json dist/ 2>/dev/null || echo 'manifest.json not in root (check public/ folder)'",
    "watch:ext": "vite build --watch",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "pdfjs-dist": "^5.4.530"
  }
}
