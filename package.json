{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:extension": "vite build && npm run copy-manifest",
    "copy-manifest": "cp manifest.json background.js content.js dist/ && cp -r icons dist/",
    "preview": "vite preview"
  }
}
